# CS Loadout Environment Variables

# Steam Community Market API (FREE - No key required)
# Public API endpoint: https://steamcommunity.com/market/priceoverview/
# No authentication needed for basic price data

# CSFloat API (FREE tier available)
# Get your API key from https://csfloat.com/
# Optional - app works without it, but provides enhanced features with key
NEXT_PUBLIC_CSFLOAT_API_KEY=

# Database (PostgreSQL - Price Caching & User Features)
# Production: Use Neon (https://neon.tech) or Supabase (https://supabase.com) free tier
# Development: Use local PostgreSQL or cloud provider
# See SETUP_DATABASE.md for detailed setup instructions
DATABASE_URL=postgresql://username:password@localhost:5432/csloadout

# Cron Job Security (Production Only)
# Generate a random secret for Vercel Cron authentication
# Example: openssl rand -base64 32
# Only required in production - leave empty for local development
CRON_SECRET=

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
